<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>MoobitRecog</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSAppTransportSecurity</key>
	<dict>
	  <!-- Do not change NSAllowsArbitraryLoads to true, or you will risk app rejection! -->
		<key>NSAllowsArbitraryLoads</key>
		<false/>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSCameraUsageDescription</key>
	<string>The camera watches you exercise so the app can detect squats, push-ups and lunges. Video is processed on-device and never leaves your phone.</string>
	<!-- iOS 14+ gates any connection to a private-network address behind a user prompt, and the
	     prompt requires this key. Without it the phone silently cannot reach the Mac, which
	     breaks BOTH the Metro bundle over Wi-Fi and the telemetry POST — and the failure looks
	     like an unexplained timeout, not a permission problem. The app only ever talks to
	     loopback and RFC1918 addresses; see isLocalHost() in src/app/session/telemetry.ts, which
	     refuses anything else. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Development builds load JavaScript from, and send debug session logs to, a dev server running on your own computer on this network. Nothing is sent to the internet.</string>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<!-- Portrait only, on purpose. The camera is propped on the floor in portrait for the
	     extra vertical headroom a full standing body needs at 6ft, and every calibration
	     baseline is captured in that one orientation. Allowing rotation would silently
	     invalidate a session's baseline mid-set. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<false/>
</dict>
</plist>
